import styled from "styled-components";

export const StyledNoTech = styled.div`
    padding: 50px;
    background-color: var(--grey3);
    border-radius: var(--radius);

    p {
        width: 100%;
        text-align: center;
        font-size: 18px;
    }
`